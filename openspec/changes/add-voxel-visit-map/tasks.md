# 작업과 검증

## 방문 볼륨 화면 개편 — 최신 검증 (2026-09-18)

- [x] 상대 방문 비율을 고정 5단계로 변경하고 범례·색·높이를 연결. 경계값과 최댓값 독립성, 군집 폭·50% 커버리지 확산 검증.
- [x] 부드러운 사각 블록을 InstancedMesh로 렌더. 레이어 재생성과 화면 종료 때 geometry 및 인스턴스 GPU 버퍼 해제.
- [x] 지역 선택·프로필·제목·일정 유형 필터·레이어·현재 위치/포커스·월별/최근 장소·하단 탐색 구현.
- [x] 서울/다른 시·도 상세 링크가 전달한 지역을 통계 화면에서 검증 후 선택하도록 연결. 잘못된 쿼리는 전국 화면 유지.
- [x] 전체 단위 테스트 318개, 구조 검사 143개 모듈, 운영 빌드 통과. Impeccable detector `[]`.
- [x] 별도 4300 test 앱에서 360×740/1280×900 빈 상태·129회/10개 시·도(59%)·서울 48회·카페 16회·월별 집계·최근 장소·큐브 직접 선택·레이어·확대·초기화·키보드·위치 허용/거부 모의 응답·503 재시도 검증. 가로 넘침 없음, 지도 버튼 44×44px, pageerror 0건. 위치 허용 검증은 브라우저 모의 좌표이며 실기기 GPS 검증은 아니다.
- [x] 독립 코드 검토에서 확인한 상세 링크 연결과 InstancedMesh 자원 해제 지적을 코드 확인 후 수정. 최종 브라우저에서 서울 구별·부산 장소·무효 지역 쿼리 확인.

최종 스크립트: `output/playwright/check-map2-volume.js`. 캡처: `map2-volume-{360,1280}.png`; 빈 상태 캡처는 제목 크기 보정 전 기록이다. 검증용 여행·인증 저장소는 원복했다. 전체 기존 E2E 묶음을 재실행한 것은 아니다. 초기 번들 625.47kB 예산 경고와 LoginPage 미사용 import 2건이 남는다. 배포하지 않았다.

미지원: 전국 시군구 경계 자동 확대, 문화·예술 분류, 사진 연결. 서울 구별 통계는 기존 화면으로 이동한다. 실제 데이터에 가상 방문이나 분류·좌표를 추가하지 않았다. 기획안, 제안·설계·요구사항, 개발 안내·문서 목차·디자인 기록을 갱신했다.

이하 기록은 이전 작업 이력이다.

## 후속 통합 검증 (2026-09-18)

- [x] 서울 자치구 상수를 model 계층으로 분리해 lint 위반 2건 해결. 구조 검사 142개 모듈 통과.
- [x] `/lab/map`의 사각 격자와 육각 렌더러 타입 불일치를 기존 `projectRegions` → `buildHexGrid` 경로로 연결해 해결. 운영 빌드 통과.
- [x] 삭제된 `placePillars` 호출과 사각 격자 테스트 데이터를 현재 `placeBlocks`·육각 좌표로 갱신. 두 지역 군집이 겹치는 경우 블록 중복이 없는지 확인. 전체 단위 298개 통과(수정 전 5개 실패).
- [x] 별도 4300 test 앱에서 `/lab/map` 렌더·서울 예시 48회 선택·장소 목록·360px 모바일 가로 넘침 없음을 확인. 캡처: `output/playwright/resume-record-map-{desktop,mobile}.png`.

기획·집계 규칙 변경은 없다. 초기 번들 625.63kB 예산 경고와 LoginPage 미사용 import 2건은 남는다. `/lab/map2` 재검증과 배포는 수행하지 않았다. 아래 lint 실패 기록은 수정 전 이력이다.

## 지형 회색 음영 보정 (2026-09-18)

- [x] 첨부 원본에 맞춰 밝은 보조광·낮은 방향광 대비·연한 접지 그림자 적용.
- [x] 360/720/1280px Playwright 검증 통과: 집계·큐브/핀 선택·확대/축소·이동·초기화·키보드·실패 후 재시도. 가로 넘침 없음, pageerror 0건, 테스트 저장소 원복. 최신 모바일 캡처에서 밝아진 지형 음영 확인.

## 둥근 블록 보정 (2026-09-18)

- [x] 흰 지형·초록 군집 공통 큐브의 모서리 반경을 0.045에서 0.18로, 곡면 분할을 2에서 3으로 조정. 무광 질감과 연한 바닥 그림자 적용.
- [x] 360/720/1280px 브라우저 재검증: 집계 118회·6개 지역 유지, 선택·확대/축소·이동·초기화·키보드·실패 후 재시도 통과. 가로 넘침 없음, pageerror 0건. 모바일 캡처 육안 확인, 테스트 저장소 원복.

집계·좌표·군집 배치는 변경하지 않았다. 기획안 및 OpenSpec 제안·설계·요구사항에 둥근 모서리 표현을 반영했다.

## 원본 2.5D 블록 재현 — 최신 검증

- [x] 흰 큐브 지형·분리된 초록 계단 군집·소유 지역 매핑 구현
- [x] 원본에 맞춘 카메라·바다·그림자·핀 라벨·높이 범례 적용
- [x] 군집 단위 테스트 및 기존 집계 회귀 74개 통과
- [x] 데스크톱·모바일 빈 상태/방문 상태 및 조작 검증

14km 샘플링으로 513개 육지 셀과 17개 시·도를 유지한다. 새 군집 테스트 5개는 빈 집계, 계단식 층수, 소유 지역, 무효 집계, 인접 군집과 겹치는 라벨 높이를 검증한다. 라벨 높이 회귀 테스트가 0/4 불일치로 실패하는 것을 확인한 후 실제 렌더 층을 읽도록 수정해 통과했다. 독립 검토의 기본 지형 범례 지적도 반영했다.

브라우저: 360/720/1280px, 테스트 집계 6개 지역·118회·미분류 1개, 초록 큐브 직접 클릭·핀 선택·확대/축소·이동·초기화·키보드·503 후 재시도 통과. 제주 핀 표시, 44px 조작 영역, 가로 넘침 없음과 pageerror 0건 확인. 테스트 저장소는 원복했다. 캡처 `output/playwright/map2-reference-{360,720,1280}.png`, `map2-reference-empty.png`; 재현 스크립트 `check-map2-reference.js`. 원본과 같은 지도 표현을 구현했으나 원본의 임의 국토 윤곽 대신 실제 출처 경계를 사용하며, 오른쪽 서울 상세·사진·월별 그래프는 현재 범위에 없다.

운영 빌드 통과(초기 번들 약 626kB 예산 경고, 기존 LoginPage 미사용 import 경고 2건 유지). 전체 lint는 기존 stats util→data 참조 2건으로 실패한다. Impeccable detector `[]`. 이번 작업은 이미지 생성·Higgsfield 호출·추가 비용 없이 첨부 원본을 직접 참고했다.

이하 Higgsfield·낮은 입체감 기록은 이전 작업 이력이며 현재 재현 방향은 design.md의 최신 결정을 따른다.

- [x] 기존 집계·격자·투영 함수 및 GeoJSON fixture 확인
- [x] 별도 `/lab/map2` 페이지와 실험실 링크 구현
- [x] 실제 경계 출처 연결 및 예시/실제 분리
- [x] 빈도 색상, 선택, 확대·축소·초기화 구현
- [x] 색상·기존 투영·기존 집계 단위 테스트 43개 통과
- [x] `/lab/map2` 라우트 포함 test 빌드 통과 (2026-09-18)
- [x] Playwright 실제 브라우저 데스크톱·360px 모바일 표시 확인, 콘솔 오류 0건
- [x] 예시 전환, 서울 48회 선택, 확대·초기화, 실제 빈 기록 복귀, 모바일 가로 넘침 없음 확인
- [x] 독립 코드 검토: 중요 결함 없음, 라우트 연결 필요 의견 반영

기존 stats util → data/seoul-grid 참조 2건 때문에 전체 boundary lint가 실패한다. 이번 새 기능의 참조 위반은 보고되지 않았다.

캡처: `output/playwright/map2-desktop.png`, `output/playwright/map2-mobile.png`. Impeccable detector 결과는 빈 배열이다. 빌드의 LoginPage 미사용 import 경고 2건은 기존 항목이다.

## 이전 보류 항목과 해결

- [x] Higgsfield MCP를 사용한 시각 자료 생성·적용. 이전 도구 미노출·인증 문제는 이번 세션에서 해결됐으며 생성 이미지와 적용 내역은 아래 후속 기록을 따른다.
- [x] 테스트 전용 여행 기록을 넣어 실제 저장소→집계→지도 표시 확인, 원본 복구.

## 최신 지도 전용 변경 검증 (2026-09-18)

- [x] 소개·예시 전환 제거, 공통 PageBar 적용
- [x] 앱 색상 토큰을 지도와 범례에 연결; 44개 관련 단위 테스트 통과
- [x] test 빌드 통과 (기존 LoginPage 미사용 import 경고 2건)
- [x] 실제 LocalVisitStats 경로로 테스트 저장소의 서울 48회 및 총 118회 표시 확인 후 원본 복구
- [x] 데스크톱 1280×900·모바일 360×740 실제 렌더 확인, 가로 넘침 없음, 선택·확대·초기화 확인

최신 캡처는 `output/playwright/map2-tokens-desktop.png`, `output/playwright/map2-tokens-mobile.png`다. 이전 예시 전환 검증은 변경 전 이력이며 현재 기능이 아니다. 위 미완료의 집계 종단 확인은 테스트 전용 데이터로 해결했다. Higgsfield는 인증 미완료로 사용하지 않았다.

## Higgsfield 디자인 적용 후속 작업 (2026-09-18)

위 도구 미노출·인증 기록은 이전 상태다. 이번 세션은 연결 및 잔액 확인 후 Recraft V4.1 이미지 1장을 생성했다(사전 견적 1.25크레딧). 생성 ID: `8e5bfea5-8854-49e5-b1d9-a35ddb1c9e31`.

- [x] 참고 이미지 생성·검토·프로젝트 보관
- [x] 공통 UI·Tailwind와 중성 지도 바탕, 간결한 범례 적용
- [x] 방문 지역 수·누적 방문·선택 지역 요약과 집계 기준 표시
- [x] 관련 단위 테스트 69개·test 빌드 통과, 독립 코드 검토 중요 결함 없음
- [x] 4300 테스트 앱의 빈 상태·기록 상태·선택·확대·초기화·키보드·모바일·실패 재시도 검증

실제 브라우저 확인: 360×740, 720×900, 1280×900에서 가로 넘침 없음, 지도 조작 버튼 44×44px. 격리한 저장소에 합계 118회·6/17지역·미분류 1개를 넣어 실제 집계 경로로 확인하고 원본 복원. 확대 후 선택 유지, 초기화 후 선택 해제, 키보드로 인천 0회 선택, GeoJSON 503 실패 후 재시도를 확인했다. 정상 경로의 pageerror는 0건이며 503은 의도한 실패 검증이다.

캡처: `output/playwright/map2-higgsfield-360.png`, `map2-higgsfield-720.png`, `map2-higgsfield-1280.png`, `map2-higgsfield-empty.png`. 재현 스크립트: `output/playwright/check-map2-higgsfield.js`. Impeccable detector는 `[]`. 전체 lint는 기존 `stats/util/district-match.ts`와 `visit-tally.ts`의 `data/seoul-grid.ts` 참조 2건으로 실패하며 이번 작업에서는 범위를 넓혀 수정하지 않았다. 테스트 빌드를 막던 `record-map/ui/record-scene.ts`의 재질 단일/배열 타입 처리만 최소 수정했다. LoginPage 미사용 import 경고 2건은 유지된다. OpenSpec CLI가 PATH에 없어 산출물을 직접 일관되게 갱신했다.

최종 확인: 작업 중 별도 record-map 파일의 동시 수정으로 `TURN` 미정의 오류가 일시 발생했으나 해당 수정 완료 후 `npm run build`를 다시 실행해 통과했다(2026-09-18 11:14 KST). 운영 초기 번들은 624.38kB로 500kB 예산 초과 경고가 있고 LoginPage 경고 2건도 남는다. map2는 40.26kB 지연 로딩 청크다. Three.js의 제거된 PCFSoftShadowMap 설정은 현재 구현인 PCFShadowMap으로 변경했다. 최종 브라우저 검증은 이동 후 선택 유지까지 포함해 통과했고 pageerror 0건, 그림자 경고 없음, 저장소 원복을 확인했다. 강제 503 검증의 네트워크 오류만 의도적으로 발생했다. 추가 배포는 하지 않았다.

## 2026-09-19 맞물리는 지형·저장 위치 마커 — 현재 검증

- [x] 지도 밖 UI를 유지하고 6.5km 샘플 2,403개/17개 시·도, 공유 경계의 불규칙 연속 입체 지형과 푸른 바다·흰 바닥·초록 군집 구현. 기존 고정 방문 단계와 실제 집계 유지.
- [x] 저장된 검증 좌표만 표시, 미래 일정 포함, 동일 좌표 묶음과 원래 여행 링크 구현. 제외·무효·미검증 좌표 및 유형 필터 단위 검증.
- [x] 지형 교차점의 임시 마커, 다른 지점으로 이동, 초기화/닫기/바다 해제, 확대·레이어 높이 갱신, 드래그 오선택 방지 구현. 임시 선택은 저장하지 않음.
- [x] 독립 코드 검토의 핀 좌표 어긋남과 하단 팝업 잘림 지적을 수정. 핀 끝 좌표 오차 1px 미만, 하단 팝업 지도 안 배치를 360px/1280px 브라우저에서 확인.
- [x] 최종 전체 단위 320개/34개 파일, 구조·순환 검사 146개 모듈, 운영 빌드 통과. 초기 번들 626.41kB 예산 경고와 기존 LoginPage 미사용 import 2건 유지.
- [x] 별도 4300 test 앱의 빈 상태 0핀, 테스트 집계 118회·6개 저장 좌표 핀, 동일 좌표 2항목 링크, 임시 마커 이동·단일성·확대·레이어·초기화·왕복 드래그·저장소 불변 검증. 360×740/1280×900 표시와 팝업 확인, 가로 넘침 없음, pageerror 0건. 렌더 삼각형 57,748개(해당 테스트 기록 기준).
- [x] 기획안·제안·설계·요구사항·개발 안내·문서 목차·디자인 기준·구현 계획 갱신.

재현: `output/playwright/check-map2-fitted.js`. 최종 캡처: `output/playwright/map2-fitted-map-{360,1280}.png` 및 `map2-fitted-{360,1280}.png`. 테스트 데이터는 격리 저장소에서만 사용하고 원복했다. 중간 브라우저 실패는 선택 팝업 위를 지형으로 클릭한 검증 스크립트였으며, 닫힘 완료를 기다리고 노출된 지형을 선택하도록 고쳐 통과했다. 전체 E2E 묶음·실기기 성능·배포는 수행하지 않았다. 이번 임시 마커는 검색이나 저장 기능을 포함하지 않는다. 과거 사각 큐브·InstancedMesh·자동 지역 핀 검증은 이전 구현의 이력이다.

## 2026-09-19 후속 지역 마커 정정 검증

- [x] 같은 시·도에 속한 저장 좌표를 지역 핀 하나로 집계. 지역명과 지형 앵커 사용. 개별 장소명·지도 팝업의 장소 링크 제거.
- [x] 빈 지역은 지역명 임시 핀으로 선택하고 저장 지역은 기존 핀을 활성화하여 중복 방지.
- [x] 전체 단위 321개, 구조 검사 147개 모듈, 운영 빌드 통과(기존 초기 번들 및 LoginPage 경고 유지).
- [x] 360/1280px 브라우저에서 서울의 서로 다른 저장 장소 2개 → 서울 핀 1개, 전북 임시 지역 선택 → 서울 저장 지역 선택 시 임시 핀 제거, 개별 링크 없음 확인. 테스트 저장소 원복.

스크립트 `output/playwright/check-map2-regions.js`, 캡처 `output/playwright/map2-regions-{360,1280}.png`. 이전 좌표별 마커 테스트는 수정 전 이력이다. 배포하지 않았다.

## 2026-09-19 과밀 마커·조작 렌더링 부하 검증

원인 확인: 모바일에 17개 지역 라벨이 동시에 표시되고 조작마다 고정 지형 그림자를 다시 계산했다. 같은 프레임에 20번 확대 변경을 요청해도 즉시 20회 렌더했다.

- [x] 전국 최대 6핀/확대 최대 10핀, 가까운 핀 생략, 선택·임시 지역 우선순위 구현 및 단위 검증.
- [x] 프레임별 렌더 병합, 정적 그림자 캐시, 지형 변경 시 무효화, 종료 시 예약 프레임 취소 구현.
- [x] 4300 test 앱, 360px, 17개 지역 각각 25회 조건에서 전후 측정: 표시 핀 17→6, 같은 프레임의 20회 조작에 렌더 20→1, 조작 프레임 삼각형 172,138→86,070. 지형 셀 2,403개 유지. 실기기 FPS 측정은 아니며 체감 지연 해소를 단정하지 않는다.
- [x] 360/1280px 저장 지역 핀·임시 지역 선택·중복 방지 회귀 통과, 테스트 저장소 원복.
- [x] 단위 322개/36개 파일, 구조 검사 148개 모듈, 운영 빌드 통과. 초기 번들 626.44kB와 LoginPage 기존 경고 유지.

측정 스크립트 `output/playwright/check-map2-performance.js`, 기능 회귀 `output/playwright/check-map2-regions.js`. 기획안·제안·설계·요구사항·개발 안내 동기화. 배포 및 사용자 실기기 검증은 미수행.

## 2026-09-19 전체 기록 전용 화면 검증

- [x] 여행·맛집·카페·문화·예술 탭과 유형 안내 제거. 빈 상태에서도 유형 선택 문구 제거.
- [x] 지도 집계·저장 지역 마커·지역 상세의 조회 조건을 all로 고정하고 불필요한 필터 상태/핸들러 제거.
- [x] 최종 운영 빌드 및 diff 공백 검사 통과. 기존 초기 번들 626.40kB 예산·LoginPage 경고 유지. 이번 단순 UI 제거는 별도 브라우저/전체 단위 재실행 없이 빌드와 코드 조회 조건으로 검증했다.
- [x] 기획안과 OpenSpec 제안·설계·요구사항 동기화. 배포하지 않았다.

## 2026-09-19 상단 선택·프로필 제거 검증

- [x] 맨 위 지역 select·내 프로필 아이콘 행과 해당 여백 제거. 기존 지도/지역 목록 선택 유지.
- [x] 기획안·제안·설계·요구사항 갱신.
- [x] 운영 빌드 및 diff 공백 검사 통과. 기존 초기 번들 626.35kB 예산·LoginPage 경고 유지. 단순 마크업 제거로 브라우저/전체 단위 검사는 재실행하지 않았다. 배포하지 않았다.

## 2026-09-19 지도 밝기 보정 검증

- [x] 밝은 하늘색 바다·민트 녹색 팔레트, 보조광 증가·방향광 대비 감소, 접지 그림자 불투명도 0.10 적용.
- [x] 360/1280px 브라우저 캡처를 직접 확인해 흰 지형 옆면과 초록 군집의 밝아진 표현 확인.
- [x] 렌더 측정에서 6개 표시 핀, 연속 변경 20회에 실제 렌더 1회, 그림자 자동 재계산 꺼짐 유지. 테스트 값은 scene 메모리에만 주입하고 페이지 새로고침으로 제거. 저장소 쓰기 없음.
- [x] 운영 빌드 통과. 기존 초기 번들 626.35kB 예산·LoginPage 경고 유지. 색상/조명만 변경하여 전체 단위 검사는 재실행하지 않았다.
- [x] 기획안·제안·설계·요구사항·디자인 기준 갱신. 배포하지 않았다.

캡처: `output/playwright/map2-bright-{360,1280}.png`. 스크립트: `output/playwright/check-map2-bright.js`. 캡처의 전체 지역 25회는 밝기·부하 확인용 메모리 데이터로 실제 사용자 기록이 아니다.

## 2026-09-19 통계 이전·구 지도 삭제 — 현재 검증

- [x] 현재 3D 지도와 관련 data/model/ui/util을 stats 기능으로 이동. `/stats` 기본 화면을 VisitMapPage로 연결하고 기존 SVG/목록은 `/stats/details`에 유지.
- [x] record-map 하위 파일과 전용 단위 테스트, `app/public/lab-voxel.html`, `/lab/map`·`/lab/map2` 라우트 삭제. 실험실 링크를 `/stats`로 변경. app/src와 e2e에 삭제 기능/경로 참조 없음.
- [x] 공통 PageBar, 22px 제목, 중성·흰 카드, 딥 블루 조작 스타일로 통합. 별도 5개 하단 메뉴 제거. 밝은 입체 지도·방문 단계·전체 조회·성능 최적화 유지.
- [x] 독립 검토의 기존 서울 구 deep-link 호환성과 WebGL 오류 시 접근 불가 지적 반영. 구 코드는 상세로 전달하고 오류 화면에서 일반 통계 링크 제공.
- [x] 전체 단위 311개/35개 파일, 구조 검사 142개 모듈 통과. 이전 322개 중 삭제한 record-map 전용 테스트 11개를 함께 제거했다.
- [x] 최종 운영 빌드 통과. 초기 번들 624.26kB 예산 및 기존 LoginPage 미사용 import 경고 유지.
- [x] 별도 4300 앱에서 360/1280px, 빈 기록과 저장소 집계 25회, 서울 지역 선택→구별 상세→전국 복귀, 시·도 쿼리와 구 쿼리, 삭제한 두 라우트의 미등록 경로 처리, WebGL 강제 비활성 시 대체 통계, 비로그인 보호 확인. 가로 넘침 없음, pageerror 0건. 여행/인증 저장소 원복.
- [x] 기획안·OpenSpec 제안/설계/요구사항·개발 안내·아키텍처·디자인·문서 목차·경계 README 갱신. 기존 E2E의 상세 화면 경로와 캡처 대상을 갱신했으나 E2E 전체 묶음은 실행하지 않았다.

스크립트 `output/playwright/check-stats-migration.js`, 최종 화면 `output/playwright/stats-migrated-{360,1280}.png`. 브라우저 검증은 격리한 테스트 인증과 여행 기록으로 수행했다. WebGL 실패 로그는 의도한 검증이며 실제 페이지 오류는 없다. 배포하지 않았다. 과거 `/lab/map2` 검증 기록은 이전 경로의 이력이다.
