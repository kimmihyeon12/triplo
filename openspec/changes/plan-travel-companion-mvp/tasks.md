## 1. 기획 검토
- [ ] 1.1 초기 대상은 본인과 친구들로 확정했다. 초기 기능 범위의 상세 검토를 마무리한다. 검증: 미확정 가정과 확정 범위가 문서에서 구분된다.
- [ ] 1.2 DB는 Supabase 무료 플랜부터 시작한다. 월 운영 예산과 일반 공개 범위를 확인하고 나머지 공급자 선택 기준을 기록한다. 검증: 비용 상한과 API PoC 평가 항목이 문서에 존재한다.
- [ ] 1.3 서비스명·캐릭터 방향과 3D 참조를 검토한다. 검증: 사용자 검토를 거친 화면 방향 문서가 존재한다.

## 2. 다음 설계 단계 준비
- [ ] 2.1 위 결정 후 장소·경로·지도앱 연결 검증 계획을 구체화한다. 검증: 설치/미설치, 도보/차량, 정보 누락 시나리오를 포함한다.
- [x] 2.2 확정 설계에 맞춰 구현 작업을 별도로 분해한다. 검증: 모든 작업에 완료 확인 방법이 있고 미확정 선택을 확정처럼 취급하지 않는다. (2026-09-09: `docs/알파-화면설계-v0.1.md` 5절 I1~I10에 완료 확인 방법과 함께 분해. AI·서버 연결은 후속 작업으로 구분)
- [ ] 2.3 여러 박 여행과 숙소 등록 흐름을 설계한다. 검증: 당일·1박·4박 이상, 연박, 숙소 변경, 숙소 미정, 직접 입력 숙소, 날짜 오류·중복·기간 밖 예약, 체크인 시간 충돌, 기간 단축 시 데이터 보존 시나리오를 포함한다. (수동 알파 범위는 3절에서 구현·검증됨. 체크인 시간 충돌은 경로 시간 연동 후 검증 가능)
- [ ] 2.4 복수 지역 선택과 지역 간 이동 흐름을 설계한다. 검증: 3박 4일 강릉 2박·속초 1박, 하루 두 지역, 같은 숙소에서 다른 지역 왕복, 실제 지점 간 이동시간의 중복 합산 방지, 미지원 이동의 사용자 입력·미정 표시, 체크인 충돌, 지역 순서·이동일 변경과 지역 삭제 시 데이터 보존을 포함한다. (수동 알파 범위는 3절에서 구현·검증됨. 미지원 이동 수단 입력과 이동시간 조회는 후속)
- [ ] 2.5 AI 숙소 등록 초안 흐름을 설계한다. 검증: 여러 숙소 요청, 상대 날짜 해석·날짜 미정 확인, 동명 숙소, 검색 실패·직접 입력, 추천 후보 선택, 미확인 정보 표시, 적용 전 일정 불변, 기존 숙소 교체 확인, 최신 일정 재검증, 중복 적용 방지, AI 장애 시 수동 등록을 포함한다.
- [ ] 2.6 기획안 19~23절을 화면별 입력·상태·오류 명세로 구체화한다. 검증: 날짜 미정, 전체/날짜별/숙소 보기, AI 부분 적용·되돌리기, 식사·휴식·시간 누락, 여행 중 지연, 비회원 저장·계정 가져오기·오프라인 충돌 시나리오를 따라갈 수 있다. (날짜 미정·전체/날짜별/숙소 보기·식사/휴식·비회원 기기 저장은 `docs/알파-화면설계-v0.1.md`에 명세·구현됨. AI 부분 적용·되돌리기, 여행 중 지연, 계정 가져오기·오프라인 충돌은 미작성)
- [ ] 2.7 내부 알파·AI 검증·외부 베타의 구현 범위와 검증 계획을 분리한다. 검증: 사용자 5명 중 4명의 핵심 흐름 수행 목표, 고정 조건·기록 보존·중복 방지 기준과 실기기/실제 여행 검증이 있고 실제 결과를 목표와 구분한다.
- [x] 2.8a 데이터 관계·소유권과 화면/저장 접근 분리를 설계하고, 샘플·임시 저장으로 여행 생성·장소/숙소 등록·전체 보기·재열기를 구현·검증한다. 검증: 화면 작업은 Supabase 프로젝트 없이 가능하다. (2026-09-09 완료: `app/` Angular 20, `TripRepository` 인터페이스 + localStorage 구현, Vitest 43건·Playwright 28건 통과. 상세는 3절)
- [ ] 2.8b Supabase 개발용 프로젝트와 Auth·테이블·RLS·마이그레이션을 구성하는 작업으로 분해한다. 검증: 연결 후 두 계정 간 여행·숙소 읽기/쓰기 차단, 같은 계정의 다른 기기 조회, 저장 실패 복구를 확인한다. 친구들과 실제 사용하기 전에 연결을 완료하며 무료 한도·보관 및 백업 방법도 확인한다.

## 3. 내부 알파 구현 (수동 일정 관리, 같은 기기 저장)
검증 근거: `app/`에서 `npm test`(Vitest 43건 통과), `npm run e2e`(Playwright 데스크톱·360px 각 14건, 총 28건 통과), `npm run build`(초기 번들 277kB, 예산 경고 없음). 2026-09-09 실행 결과.
- [x] 3.1 Angular 20 앱 스캐폴딩, Vitest·Playwright 설정, 런타임(4200, `tc.trips.v1`)/테스트(4300, `tc.test.trips.v1`) 분리.
- [x] 3.2 도메인 모듈: 날짜 계산(N박 N+1일, 당일 0박), 숙박 유효성·중복·기간 밖·밤별 배정·연박, 순서 변경·배치·제외·합계·지역 이동 구간·고정 시각 충돌, 기간 변경·지역 삭제 영향과 보존, 전체 보기 요약. 단위 테스트 36건.
- [x] 3.3 `TripRepository` 인터페이스와 localStorage 구현, 손상 항목 건너뛰기, 쓰기 실패 예외·기존 데이터 보존. 단위 테스트 7건.
- [x] 3.4 여행 목록·만들기·편집 화면: 빈 상태, 날짜 오류(종료<시작, 한쪽만 입력), 날짜 미정 초안, 지역 순서 편집, 기간 단축·지역 삭제 영향 확인 후 반영(자동 삭제 없음).
- [x] 3.5 여행 상세: 전체(날짜·지역·주요 일정·숙소 상태·체류 합계·이동 미확인·지역 이동 횟수·미배치·확인 필요), 날짜별(날짜 탭 키보드 좌우 이동, 그날 숙소 체크인/체크아웃/연박/미정, 시간순 항목, 이동 구간·지역 이동 표시, 위/아래 버튼 순서 변경, 제외/복원, 하루 합계, 고정 시각 충돌), 숙소(밤별 표, 숙박 카드, 중복·기간 밖 배지, 당일 여행 안내).
- [x] 3.6 장소·활동 폼(장소/식사/휴식/여유시간, 이름·주소·지역·날짜·체류시간·고정 시각·메모, 위치 미확인 고정)과 숙소 폼(체크인·체크아웃 날짜 검증, 시각 미정, 예약 여부 사용자 입력, 기간 밖·중복 경고와 확인 저장, 날짜 미정 여행 안내). 삭제는 2단계 확인.
- [x] 3.7 저장 상태(저장 중·저장됨·저장 실패·다시 저장) 표시, 실패 시 입력·메모리 상태 보존과 재시도. 이름·주소 기반 네이버지도·카카오맵 검색 링크(새 탭)와 주소 복사(클립보드 실패 시 선택 가능한 텍스트 대안).
- [x] 3.8 Playwright 시나리오: 첫 흐름·재열기, 당일 여행, 날짜 오류, 날짜 미정→확정·미배치 배치, 3박 4일 두 지역·두 숙소, 숙박 날짜 오류·중복·기간 밖, 기간 단축·지역 삭제 보존, 순서 변경·제외·합계·고정 시각 충돌, 저장 실패·재시도(폼·상세), 키보드 조작·가로 스크롤 없음. 데스크톱과 360×740에서 실행.
- [x] 3.11 디자인 리뉴얼(2026-09-09): Impeccable 방향 배정(시드 81aaffeb)으로 후보 3개(인스탁스 앨범·스티커 플래너·클린 앱 표준)를 여행 상세 목업으로 비교하고 사용자가 ‘클린 앱 표준’을 선택. 전역 토큰·서체(Noto Sans KR)·모든 화면 스타일과 지도 로딩·빈 화면·오류 상태를 교체. 검증 결과는 아래 3.12에 기록.
- [x] 3.9 Impeccable: docs/design/PRODUCT.md(문서 추론 표시), 1차 방향(시드 580ca994 표지판 체계)은 사용자가 거부해 폐기하고, 사용자 확정 방향(부드럽고 따뜻하고 귀여운 톤, 트리플처럼 깔끔하게, 트리플 색 제외)으로 교체. surface brief 방향 계약 갱신, 감지기 재실행(지적 0건), 교체 후 Playwright 28건 재통과. 마무리 리뷰·docs/design/DESIGN.md 결과는 확인되는 대로 기록한다.
- [x] 3.10 PWA 설치와 앱다운 동작: 매니페스트·서비스 워커(`ngsw-config.json`, 설정 JSON은 캐시 제외), View Transitions 화면 전환(같은 경로의 쿼리 변경은 건너뜀), 버튼 누름 반응, 오버스크롤·길게 누르기 억제. 배포 빌드는 `npm run build:deploy`가 환경 변수로 지도·Supabase 설정 파일을 만든다. 검증: 빌드 산출물에 `ngsw-worker.js`·`manifest.webmanifest`·`_redirects` 포함, 브라우저에서 매니페스트 200·`display: standalone` 확인. 실제 휴대폰 설치는 HTTPS 배포 후 확인한다.
- [x] 3.11 UI 일관성 정리: 공용 컴포넌트로 체크박스·탭·행 더보기를 추가하고 여행 상세·정산 양쪽에 적용. 여행·방문지·숙소 삭제를 확인 절차와 함께 구현. 정산 화면의 Tailwind 기본값 46곳을 토큰으로 교체. 검증: 빌드·단위 90건·경계 87모듈 통과, 브라우저에서 더보기 열림·삭제 확인·배경 처리 확인.
- [ ] 3.12 미연결·후속(사용자 우선순위 2026-09-09: 디자인 정리 → 지도 SDK·경로 표시 → 선택형 AI 일정 만들기): 실제 장소 검색·좌표·이동시간 조회, 지도앱 스킴·설치 여부 대응(현재는 웹 검색 링크), 미지원 이동수단 입력, 여행 준비 완료·여행 시작/완료 상태, 하단 4개 메뉴(발견·기록지도·여행 친구), 친구 초대·공유 화면의 토큰 정리와 기능 연결. 처음 사용자 5명 중 4명 수행 목표는 실사용 검증으로 별도 확인한다.

## 4. Supabase 연결 (2026-09-14 사용자 요청으로 Google 인증 선행)
- [ ] 4.1 개발용 Supabase 프로젝트 생성, `.mcp.json`/`.codex/config.toml` project ref 지정과 로그인. 사용자 제공 ref wslqgfetdwcmqeztixvs·공개 키 확인. 관리 도구 인증은 미완료.
- [ ] 4.2 `trips`·`trip_regions`·`trip_stops`·`accommodation_stays` 테이블과 `owner_id` RLS 마이그레이션. 알파 모델 필드를 기준으로 작성한다.
- [ ] 4.3 Supabase Auth 로그인과 `TripRepository` 서버 구현. 로컬 초안을 계정 저장으로 한 번만 가져오기.
- [x] 4.3c 카카오·Google 최초 인증 후 닉네임만 받는 가입 흐름과 기존 회원 바로 입장, 저장 검증·실패 입력 보존·버튼 스피너를 구현한다. 모의 OAuth·프로필 HTTP로 PC·모바일 흐름을 검증했으며 실제 계정 인증 완료와 구분한다.
- [x] 4.3d 오류 토스트를 하단 중앙으로 옮기고 PC·360px 실제 런타임에서 배치·닫기·카카오 버튼 활성화를 검증한다. 실제 카카오계정 화면과 Google 로그인 화면 도착 확인.
- [x] 4.3e KOE205 대응으로 카카오 동의 요청을 profile_nickname만으로 제한한다. 회귀 테스트 수정 전 실패 확인, 수정 후 PC·모바일 Google·카카오 가입 테스트 4개와 운영 빌드 통과. 실제 카카오 요청 scope 확인. 관리자 동의항목·이메일 없는 계정 허용 설정과 실계정 인증 완료는 별도 확인 대상이다.
- [ ] 4.4 두 계정 간 접근 차단, 같은 계정 다른 기기 조회, 저장 실패 복구, 무료 한도·백업 확인.

- [x] 4.3a Google PKCE 로그인·내 정보·로그아웃·인증 가드의 자동 검증과 실제 Google 로그인 화면 이동 확인. 실제 계정 인증 완료는 사용자 확인 후 기록.
- [ ] 4.3b 본인 회원탈퇴 서버와 확인 화면을 구현·배포하고 테스트 계정으로 실제 삭제 검증. 서버 인증·배포 전에는 런타임 탈퇴를 비활성화.

## 5. 여행 가계부·정산 (4절 Supabase·8절 동행 권한 이후, 사용자 요청 2026-09-09)
- [ ] 5.1 기획안 24절과 `travel-expenses` 요구사항을 화면별 입력·상태·오류로 구체화한다. 검증: 예산 없음, 날짜 미지정 지출, 숙박 요금 자동 제안, 항목 삭제 후 보존 시나리오를 따라갈 수 있다.
- [ ] 5.2 Expense 모델·합계 계산·저장소를 구현하고 숙소 폼에 요금·예약 번호·예약처를 추가한다. 검증: 단위 테스트와 Playwright 시나리오 통과.
- [ ] 5.3 기획안 27절의 결제자·부담 대상·개인/공동 범위·균등/직접 분배와 잔액·송금 제안을 구현한다. 검증: ABC 예시, 1원 나머지, 불참자, 중복 이름, 합계 오류, 멤버 변경 후 과거 분담 보존, 전체 잔액 0원.
- [ ] 5.4 수동 수령·일부 수령·취소 이력과 정산 후 지출 변경 재계산을 구현한다. 검증: 초과 수령 차단, 반환 필요 금액, 수령액의 여행 총액 중복 합산 방지, 새 지출 후 미정산 전환, 버전 충돌·재시도 중복 방지·원자적 저장.
- [ ] 5.5 경비·정산 화면과 권한을 검증한다. 검증: 소유자만 장부 수정, 승인된 동행만 공동 내역 조회, 개인 지출·예약번호 누출 차단, 제거된 계정 직접 조회 거절, 입력 보존, 360px·키보드 사용. 실제 은행 확인이 아닌 수동 기록 표시.

## 6. 트리플 참고 선택형 AI 일정 (7절 지도 연결에 이어 진행, 2026-09-09)
- [ ] 6.1 `docs/트리플-맞춤일정-분석.md`와 기획안 25절을 기준으로 Angular 단계형 입력 화면을 구현한다. 검증: 복수 지역·실제 날짜/기간·동행·취향·이동/밀도, 입력 보존, 날짜 미정, 수동 작성 대안, 오류·취소 복구.
- [ ] 6.2 지도·날짜 탭·번호 카드·선택 수·고정 담기 결과 화면을 구현한다. 검증: 첫 결과 전체 선택, 날짜별/전체 부분 선택, 0개 선택, 상세 왕복, 같은 장소 다른 날짜를 각각 제어.
- [ ] 6.3 선택 결과 적용·경로 갱신·교체/재추천을 연결한다. 검증: 선택 항목만 담기, 클릭마다 경로 API 미호출, 오래된 경로 표시, 기존 숙소 보존, 원자적 적용·중복 방지·실패 복구·되돌리기.
- [ ] 6.4 리뉴얼된 클린 앱 표준 디자인(docs/design/DESIGN.md)을 유지하며 참고 페이지와 유사한 위계·여백·카드·고정 버튼을 검증한다. 검증: 360px와 PC, 키보드·포커스·선택 대비, 지도/목록 상태 일치, 샘플과 실제 연동 구분. 네이티브 트리플 앱 미검증 범위를 구현 근거와 혼동하지 않는다.
- [ ] 6.5 로컬 개발용 AI 서버와 로컬 LLM(사용자 결정 2026-09-09, 외부 유료 API 없음)을 연결한다. 검증: 서버 비밀키의 클라이언트 미포함, 요청·응답 검증, 검증된 후보만 사용, 요청 횟수·크기 제한, 실패 시 입력 보존, 실제 생성→선택 해제→선택 항목 담기 1회 이상. 샘플/실제 호출 결과와 비용을 구분한다.
  - 2026-09-10 준비: `docs/LOCAL-LLM.md`에 RTX 3090 24GB용 Ollama·qwen3.5:9b 후보와 설치/사설 연결 절차를 기록했다. 실행 PC 위치·주소, 설치·GPU 적재·실응답, 앱 서버·화면 연동은 아직 미검증/미완료다. 위 체크박스는 실제 선택 저장 흐름까지 검증한 뒤 갱신한다.

## 7. 지도·장소·경로 실제 연결 (다음 개발 작업, 6절보다 먼저)
- [x] 7.1 지도 표시·장소 검색 제공자 확인·결정(2026-09-09): 카카오맵 JavaScript SDK + services 키워드 검색. 무료 한도(첫 활성화 앱, 지도 Web SDK 일 300,000건·키워드 검색 일 100,000건, 결제 수단 불필요)·필수 설정([카카오맵] > [사용 설정] ON, JavaScript 키, Web 플랫폼 도메인 등록)을 `docs/HARNESS.md`에 기록. `app/public/app-config.example.json`과 `MapProvider`/`PlaceSearchProvider` 어댑터 준비, 실키는 gitignore. 도보/차량 경로 제공자는 7.3에서 확인한다.
- [ ] 7.2 Angular에 실제 지도와 장소 검색을 연결한다. 검증: 확인된 좌표만 표시, 날짜 탭·장소 카드·번호 마커·선택 상태 동기화, 장소 선택과 숙소 지점 표시, 360px·PC에서 조작 가능. (2026-09-09 진행: 카카오 어댑터·검색 상자·날짜별/숙소 지도·마커↔카드 동기화 구현. 픽스처 제공자로 Playwright 4건 통과(검색→선택→저장, 빈 결과·직접 입력, 위치 지우기, 숙소 마커). 실제 카카오 키로의 실데이터 검증은 사용자의 JavaScript 키 입력 후 진행하며 아직 미완료)
- [ ] 7.3 실제 경로·시간을 연결하고 누락·실패·선택 변경 시 오래된 결과를 구분한다. 검증: 차량/도보 각각 지원 여부 확인, 단순 안내선을 경로로 표시하지 않음, 요청마다 재계산하지 않음, 미확인 시간 생성 금지.
- [ ] 7.4 지도·검색·경로를 실제 데이터로 검증한 뒤 6절 선택형 AI 화면에 연결한다. 검증: 화면/서버 실행 방법, 테스트 앱과 포트·저장소 분리, 외부 호출을 모킹한 자동 테스트와 실제 API 검사를 별도 기록. 서버 저장·RLS·인증 기반 호출 제한은 4절에서 친구 사용 전 완료.

## 8. 동행·선택형 일정 공유 (4절 Supabase 이후, 5절 공동 정산의 선행 작업)
- [ ] 8.1 기획안 27절의 공유 미리보기·만료·해제·초대 승인·멤버 관리·권한 없음 상태와 데이터 관계를 구체화한다. 검증: 공개 링크와 로그인 동행의 차이, 소유자 쓰기·동행 읽기, 기본 비공개 필드가 명시된다.
- [ ] 8.2 TripParticipant·TripMember·TripInvite·TripShare와 RLS·공유용 응답 경계를 구현한다. 검증: 계정 권한과 회계상 사람 분리, 기본 7일·일회용 초대·소유자 승인, 비공개 원본 테이블 차단, 제거 후 직접 요청 거절, 미가입 친구 계정 연결 확인.
- [ ] 8.3 선택한 일정의 스냅샷·미리보기·링크 복사·명시적 갱신·만료·해제를 구현한다. 검증: 기본 전체 선택 후 일부 제외, 숙소 별도 동의, 사진·예약번호·정산 누출 없음, 원본 수정 분리, 토큰 해시 저장·추측 불가·만료/해제 검사·no-store. 메시지를 자동 발송하지 않는다.
- [ ] 8.4 소유자·승인 동행·미승인/제거 계정·비로그인 링크 방문자의 통합 흐름을 검증한다. 검증: 360px·키보드, 링크 재사용·재발급·초대 만료, 타 여행 ID 요청, 최신 갱신 시각·오프라인 실패·입력 보존. 기획 완료와 서버 권한 실증을 구분한다.
- [ ] 8.5 코드로 참여를 구현한다. 검증: 8자리 코드·복사·정규화, 여러 계정의 같은 코드 요청, 로그인 후 이어가기, 여행당 활성 코드 한 개·HMAC 저장·시도 제한, 만료·재발급 시 대기 요청 차단, 소유자 승인·중복 방지·이미 참여한 계정·거절/제거 후 재참여·기존 회계 기록 보존.
- [ ] 8.6 현재 디자인 시스템을 기준으로 상단 D-day·기간·지역 구분·지출·동행 프로필을 구체화하고 화면을 구현한다. 검증: 날짜 미정·당일·여행 중·예정일 경과·한국 시간 자정, 지출 미연결/조회 실패/실제 0원 구분, 개인·공동 합계 권한, 실제 멤버와 대기/미연결/샘플 프로필 구분, [+ 초대]→코드/링크 진입. 화면 준비는 서버 연결 전 가능하며 실제 참여는 8.2·8.5 검증 후 제공.

## 9. Angular 21·아키텍처·상태관리 정리 (2026-09-14 적용)

2026-09-14 사용자 요청으로 적용·검증했다. 아래 완료 범위는 로컬 내부 알파이며 실제 계정·서버 검증은 후속 단계다.

- [x] 9.1 현재 코드와 다른 작업자의 변경을 확인하고 빌드·Vitest·핵심 Playwright 기준 결과를 확보한 뒤 Angular core/CLI 및 관련 패키지를 21.x로 공식 마이그레이션한다. 검증: Node·TypeScript·RxJS 호환, lockfile, 런타임/테스트 빌드, 저장·재열기·지도 흐름. 전역 CLI 교체와 기존 테스트 러너 교체를 필수로 묶지 않는다.
- [x] 9.2 docs/architecture/ARCHITECTURE.md의 상태 계약을 구현한다. 검증: 조회 응답 역전, 연속 저장 중 새 편집, 이전 성공이 최신 실패/입력을 지우지 않음, 여행별 실패 입력 보존·재시도, 동일 화면 여행 ID 전환, 계정 변경 시 상태 정리. 서버 충돌 검증은 9.5에서 별도 수행한다.
- [x] 9.3 NgRx Signals 21.x를 도입하고 목록·여행 작업 화면 서비스를 분리한다. 기능별 feature/ui/data/model/util로 점진 이동하며 큰 상세 화면과 공통 UI 의존성을 정리한다. 검증: private signalState, UI의 직접 저장소 변경 금지, 장소·숙소 자식 화면 상태 공유, 순수 함수 기존 테스트, 금지 import·순환 참조 lint.
- [x] 9.4 Zoneless를 별도 전환·검증한다. 검증: provideZoneChangeDetection·ZoneJS polyfills·테스트 설정 일관성, 지도 SDK 콜백·Reactive Forms 프로그램 변경·저장 결과 화면 갱신, 모바일 핵심 E2E. Signal Forms·rxResource는 전면 교체하지 않고 필요 시 단일 흐름 실증 후 결정과 결과를 기록한다.
- [ ] 9.5 Supabase 연결 단계에서 용도별 저장 명령과 버전·재시도 식별자·트랜잭션을 검증한다. 검증: 여러 탭의 오래된 저장 거절, AI 적용 중복 방지, 승인·정산 원자성, 권한별 데이터 차단. 4·5·6·8절의 관련 서버 검증과 통합하고 중복 작업하지 않는다.

검증 기록 (2026-09-14): Angular core 21.2.23 / CLI 21.2.24 / NgRx Signals 21.1.1. 공식 core·CLI migration-only 완료. 운영 빌드 통과(초기 271.56 kB), test 구성 앱 빌드·Playwright 40/40(PC·360px), Vitest 70/70, 경계·순환 참조 lint 50모듈 통과. 테스트 서버는 4300과 테스트 저장 키·fixture를 사용하고 watch/live-reload를 꺼 동시 편집으로 화면이 초기화되지 않도록 했다. 실제 카카오 키 연동은 다른 세션에서 진행하므로 이 결과에 포함하지 않는다.

9.2의 세션 검증은 PendingDraftRegistry 세션 변경·이전 세션 commit 거절의 단위 검증이다. 실제 로그인/로그아웃 연동과 폼 정리는 Supabase 단계에서 검증한다. 9.4는 현재 사용 중인 signal·ngModel 폼과 fixture 지도 비동기 갱신을 브라우저에서 검증했다. 실제 지도 SDK 콜백은 지도 연동 단계에서 별도 실증하며 Reactive Forms·Signal Forms·rxResource 도입은 이번 범위에 없다.

## 10. 이름 중심 로고 시범 적용 (2026-09-10)

- [x] 10.1 사용자 요청에 따라 그림 심볼을 제거하고 ‘트립플로’ 워드마크와 T favicon을 적용했다. test 구성 개발 서버 빌드 및 Playwright PC 1440px·모바일 360px 검증 완료: 이름 표시, 그림 심볼 없음, 가로 넘침 없음, 최소 44px 조작 영역, 홈 이동, 키보드 포커스, 강제 색 모드, favicon HTTP 200. 증거: output/playwright/verify-wordmark.js 및 wordmark-*.png. 당시 적용·검증 이력이다. 현재 상단은 T 심볼과 화면 제목을 사용하며 DESIGN.md를 따른다.

## 인증 검증 기록 (2026-09-14)

 Vitest 85개, PC·360px 전체 Playwright 86개 통과. 운영 빌드 성공(초기 272.27 kB), 의존성 경계·순환 55모듈 통과. 토스트 하단 배치·닫기는 실제 런타임의 PC·360px에서 별도 확인했다. Google·카카오 실제 제공자 로그인 화면 도착까지 확인했으며, 실제 사용자 인증 완료·닉네임 서버 저장·회원탈퇴 함수 배포·여행 DB/RLS는 후속 확인 대상이다.

## 11. Tailwind 디자인 시스템과 실험실

- [x] 11.1 Tailwind PostCSS와 단일 테마 토큰을 구성하고 기존 화면 스타일을 HTML Tailwind 유틸리티와 공통 호스트 클래스 모음으로 전환하고 화면별 CSS를 제거한다.
- [x] 11.2 모든 인라인 템플릿·스타일을 분리하고 정적 검사로 재발을 방지한다.
- [x] 11.3 버튼·입력·필드·로딩·배지·안내·하단 동작 바를 공통화하고 제품 화면에서 사용한다. 온보딩을 기존 폼 규칙과 통일한다.
- [x] 11.4 실험실 /lab에 실제 공통 컴포넌트와 토큰의 인터랙티브 디자인 시스템을 추가하고 진입 링크를 연결한다.
- [x] 11.5 PC·360px 화면 비교, 키보드·인증·여행 편집 회귀, 운영 빌드·단위·경계 검사·OpenSpec을 검증하고 디자인/구조 문서를 갱신한다.
- [x] 11.6 HTML의 Tailwind 권장 표기 경고를 수정했다. 15개 템플릿의 657개 표기를 설치된 Tailwind 기준으로 변환하고 중복 클래스를 제거했다. 재검사에서 추가 변환 0개, 운영 빌드·69모듈 경계 검사·전체 PC/360px E2E 94개 통과. 온보딩·실험실의 주 버튼 색상과 가로 넘침도 별도 확인했다. 작성 기준은 DESIGN.md에 반영했다.

2026-09-14 Tailwind 최종 검증: 전체 Playwright 92개 통과 후 스피너 회전 수정과 관련 PC·360px 회귀 10개를 추가 실행해 통과했다. Vitest 85개, 아키텍처 경계·순환 검사 69모듈, 운영 빌드(초기 323.09 kB), OpenSpec strict 검증 통과. 실제 4200 앱에서 PC 1280px·모바일 360px의 온보딩·로그인·여행 폼·실험실을 확인했고 런타임 오류·가로 넘침이 없었다. 비로그인/닉네임 미완료 각각 7경로의 미리보기 이동, 모의 서버 닉네임 저장 후 여행 이동을 확인했다. TS 99파일에서 연속된 빈 줄이 없음을 검사했다. 실제 제공자 계정 인증 완료·회원탈퇴 서버 배포·여행 DB/RLS는 이번 UI 검증 범위 밖이며 기존 미완료 상태를 유지한다.

## 12. 친구 초대·비용·이미지 화면 설계 및 로컬 구현

- [x] 12.11 기간 달력 상단의 시작일/종료일 박스를 날짜 요약과 활성 밑줄로 정리. PC/360px 범위 선택·취소·초기화·저장 검증과 운영 빌드 통과. 다른 세션의 일시적인 여행 탭 타입 오류가 해소된 현재 코드로 재검증했다.

- [x] 12.6 뒤로가기 있는 화면의 공통 상단 제목을 왼쪽 정렬한다. 360px·1280px 각각 7개 화면의 제목 시작 위치(뒤로 영역 다음 4px), 가로 넘침·런타임 오류 없음, 운영 빌드 통과 확인.

최신 사용자 지시: DB 연결 없이 디자인과 기획을 함께 진행한다. 서버 구현·마이그레이션·실제 초대/동기화는 이번 범위 밖이다. 기존 localStorage 여행과 공통 디자인 시스템을 사용한다.

- [x] 12.1 일정의 예상 비용과 실제 지출 모델을 분리하고 정수 분담·잔액·수령/취소 계산을 단위 검증한다.
- [x] 12.2 친구 초대 화면과 비로그인/참여자 공유 미리보기를 구현한다. 실제 공유 링크 생성·전송·권한을 구현 완료로 표시하지 않는다. 비로그인 미리보기는 개인 메모·가계부를 제외한다.
- [x] 12.3 일정 예상 비용 입력·합계와 가계부의 실제 지출 등록·수정·삭제·결제자·분담·정산 화면을 구현한다. 이 기기에 저장하며 예상 금액을 실제 지출로 자동 합산하지 않는다.
- [x] 12.4 전체/날짜별 PNG 일정 저장 화면과 내보내기를 구현한다. 예상 비용 포함 선택, 긴 이름 줄바꿈, 개인 메모·지도 타일·조작 버튼 제외를 검증한다.
- [x] 12.5 HTML/TS 분리·Tailwind·기존 공통 UI·로딩/하단 토스트를 적용하고 PC·360px에서 화면·키보드·실제 다운로드를 확인한다. 단위·경계·빌드·OpenSpec 결과와 후속 DB 연동을 문서에 구분한다.


2026-09-14 로컬 디자인 검증: 단위 90개 통과, 경계·순환 82모듈 통과. 전체 E2E 94개 중 92개 통과 후, 다른 세션에서 변경된 로그인 로딩 화면(버튼 미노출)에 맞춰 오래된 검사 1개를 수정하고 PC/360px 관련 회귀 8개 모두 통과했다. 새 기능은 별도 4301 테스트 앱에서 PC 1280px·360px 각각 초대/게스트/참여자 미리보기, 예상 비용 재열기, 실제 지출 CRUD·재열기, 균등 분담·부분 수령/취소, 저장 실패 입력 보존, 참여자 추가 시 작성 보존, 변경 전 정산액 수령 거부, 비로그인 조회, PNG 파일·폭 1000px를 확인했다. 운영 빌드와 OpenSpec strict 통과. 실제 DB 저장·초대 링크·동기화는 구현하지 않았다.

- [x] 12.7 이동 로딩을 200ms 지연 고정 표시로 변경한다. PC/360px 지연 이동에서 기존 화면 좌표 동일·완료 후 제거·캐시 이동에서 로딩 깜박임 없음 확인.
- [x] 12.8 공통 select 디자인과 텍스트 검색 버튼을 적용하고 PC/360px 키보드·검색 회귀·빌드를 검증한다.

- [x] 12.9 날짜·시간 공통 선택창과 기본/하단 버튼 44px 통일. 직접 입력·선택·제한 범위·키보드·저장을 검증한다.

공통 입력 검증: PC 1280px·360px 각각 select/input 테두리·44px 높이·10px 모서리 일치, 열린 옵션 폰트·둥근 패널·선택, 텍스트 검색 버튼을 확인했다. 달력 min/max·월 이동·방향키·선택 저장, 시간 시/분 적용·직접 입력 후 저장, AI로 만들기/장소 추가 실측 44px 확인. 관련 회귀 24개 중 22개 통과 후 새 select의 목록 열기/Enter 확정 방식에 맞춰 키보드 테스트를 갱신했고 해당 PC/360px 2개 재실행 통과. 장소 검색 관련 10개는 앞선 실행에서 통과했다. 최종 운영 빌드·83모듈 경계 검사·OpenSpec strict 통과. select 패널 꾸미기는 지원 브라우저에서만 적용하며 미지원 브라우저에서는 네이티브 목록을 유지한다.

- [x] 12.10 여행 기간 범위 선택과 하단 설명 6px·공통 연한 색상 적용. PC/360px에서 월을 넘는 범위·선택 중 취소·역순 재선택·당일·저장/재열기·초기화 검증. 이름·기간·지역 설명 간격 6px, 기간 박스 44px, 가로 넘침·런타임 오류 없음 확인. 관련 E2E 20개와 83모듈 경계 검사 통과.

- [x] 12.12 기간 입력 테두리·좌우 12px 패딩·설명과 숙박 라벨 사이 8px 간격을 정리하고 달력 범위를 둥근 양 끝으로 연결한다. PC/360px 화면 및 범위 선택·취소·초기화·저장 검증과 운영 빌드 통과.
