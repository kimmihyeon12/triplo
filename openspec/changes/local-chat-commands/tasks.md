# 구현 계획 (writing-plans)

- [x] 순수 로컬 명령 테스트 작성 및 실패 확인
- [x] 조회·대상 선택·일정 변경 해석기 및 상태 비교 초안 구현
- [x] 경비 명령과 저장·되돌리기 연결
- [x] 이동 링크·텍스트 복사 UI 연결
- [x] 기획안·문서 목차·검증 안내 갱신
- [x] 단위·스토어 테스트, 빌드·구조 검사, 별도 4300 앱 브라우저 검증

수정 지점: travel-chat/model/chat.ts(로컬 초안), util/local-commands 및 local-command-draft(순수 처리), data/travel-chat-store.ts(라우팅·가계부 저장), ui/chat-thread(링크/복사), 신규 테스트. 기존 provider 응답 형식은 확장하지 않는다.

## 검증 결과 (2026-09-22)

- 채팅 단위·스토어 8개 파일, 107개 테스트 통과. 오래된 여행·가계부 초안, 저장 실패, 이후 편집 후 undo, 부정·동명·범위, 미배치 순서, 예산 포함 추천 라우팅을 포함한다.
- 운영 빌드 통과. 구조 검사 170개 모듈 통과. CRLF를 허용한 diff 공백 검사 통과.
- 별도 4300 test 앱, Chromium 1280px 및 360px에서 AI 실패 플래그 `tc.test.chatFail=1` 상태로 체류시간 확인·적용, 채팅 되돌리기, 날짜 이동, 경비 저장·합계 조회, 실제 클립보드 복사, 날짜 화면 링크와 새로고침 후 저장 유지를 확인했다. 런타임 오류 및 가로 넘침 없음.
- 모바일 확인 카드에서 값이 잘리는 것을 발견해 로컬 변경만 전후를 세로 배치하고 줄바꿈하도록 수정·재확인했다.
- 검증 스크립트(로컬 산출물): `output/draw/check-local-commands.mjs`. 스크린샷: `output/playwright/local-confirm-360.png`, `local-confirm-1280.png`, `local-commands-360.png`, `local-commands-1280.png`.
- 코드 검토 지적 4건 수정: 명시하지 않은 전체 삭제 차단, 이름 대상에도 날짜·분류 조건 적용, 예산 언급 추천의 AI 경로 유지, 미배치 이동 시 순서 재정렬.
- 남은 빌드 경고: 초기 번들 769.97kB/500kB, LoginPage 미사용 import 2건. 마지막 미배치 조회 라우팅 보완 후에도 채팅 107개 테스트와 운영 빌드를 재확인했다.

외부 날씨·실제 이동시간·검색 연결은 이 변경 범위 밖이다. 다중 참여자 결제자·복잡한 분담 수정과 모호한 명령은 기존 화면/지원 형식으로 안내한다. 자세한 지원 범위는 `docs/local-chat-commands.md`를 따른다.
